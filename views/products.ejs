<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Bakery App</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Margarine&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Rubik:ital,wght@0,300..900;1,300..900&display=swap" rel="stylesheet">

</head>
<body>
    <%- include ('partials/navbar.ejs') %>

    <div id="carousel" class="carousel slide" data-bs-ride="carousel">

        <!-- Indicators/dots -->
        <div class="carousel-indicators">
          <button type="button" data-bs-target="#carousel" data-bs-slide-to="0" class="active"></button>
          <button type="button" data-bs-target="#carousel" data-bs-slide-to="1"></button>
          <button type="button" data-bs-target="#carousel" data-bs-slide-to="2"></button>
        </div>
        
        <!-- The slideshow/carousel -->
        <div class="carousel-inner">
          <div class="carousel-item active">
            <img src="/images/doughnut.jpg" alt="image" class="d-block" style="width:100%">
          </div>
          <div class="carousel-item">
            <img src="/images/cupcake1.jpg" alt="image" class="d-block" style="width:100%">
          </div>
          <div class="carousel-item">
            <img src="/images/cookie.jpg" alt="image" class="d-block" style="width:100%">
          </div>
        </div>
        
        <!-- Left and right controls/icons -->
        <button class="carousel-control-prev" type="button" data-bs-target="#carousel" data-bs-slide="prev">
          <span class="carousel-control-prev-icon"></span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#carousel" data-bs-slide="next">
          <span class="carousel-control-next-icon"></span>
        </button>
    </div>
    
    <div class="container mt-3">
        <h2 class="top-word">HOME</h2>
        <div class="row"> <!--make it show as a row-->

            <% for(let i = 0; i < products.length; i++) { %>
                
                <div class="card mx-auto" style="width:400px">
                  <img class="card-img-top" src="/images/<%= products[i].image %>" alt="Product Image" style="width:100%">

                  <div class="card-body">
                    <h4 class="card-title"><%= products[i].name %></h4>
                    <p class="card-text"><%= products[i].description %></p>
                    <p class="card-text">Price: $<%= products[i].price %></p>
                    <a class="btn cart-btn" href="/cart/add?productId=<%= products[i].productId %>">Add to Cart</a>
                    <a class ="btn review-btn" href="/reviews/<%= products[i].productId%>">Reviews</a>
                  </div>

                </div>
            <% } %>

        </div>

    </div>
</body>
</html>