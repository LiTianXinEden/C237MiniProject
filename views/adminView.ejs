<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <title>Bakery App - Edit Products</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Margarine&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Rubik:ital,wght@0,300..900;1,300..900&display=swap" rel="stylesheet">
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    
    <div class="container mt-3">
        <h2 class="top-word">Admin View</h2>
        <div class="row">

            <% for(let i = 0; i < products.length; i++) { %>
                <div class="card mx-auto" style="width:400px">
                    <img class="card-img-top" src="/images/<%= products[i].image %>" alt="Product Image" style="width:100%">
                    <div class="card-body">
                        <h4 class="card-title"><%= products[i].name %></h4>
                        <p class="card-text"><%= products[i].description %></p>
                        <p class="card-text">Price: $<%= products[i].price %></p>
                        <a class="btn edit-btn" href="/editProduct/<%= products[i].productId %>">Edit</a>
                        <a class="btn remove-btn" href="/deleteProduct/<%= products[i].productId %>" onclick="return confirm('Are you sure you want to delete this product?')">Remove</a>
                    </div>
                </div>
            <% } %>
            
        </div>
    </div>
</body>
</html>
