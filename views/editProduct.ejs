<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Cart</title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://fonts.googleapis.com/css2?family=Margarine&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Rubik:ital,wght@0,300..900;1,300..900&display=swap" rel="stylesheet">
</head>
    
</head>
<body>
    <%- include ('partials/navbar.ejs') %>

    <div class="container mt-3">
        
        <div class="container">
            <div class="row justify-content-center mt-4">
                <div class="col-md-6">
                    <h2 class="top-word">Update Product</h2>
                    <br>
                    <% if (product) { %>
                        <form action="/editProduct/<%= product.productId %>" method="POST" enctype="multipart/form-data">
                            <label for="name">Product Name:</label><br>
                            <input type="text" id="name" name="name" value="<%= product.name %>" class="form-control" required><br>
            
            
                            <label for="price">Price:</label><br>
                            <input type="number" id="price" name="price" min="0" step="0.01" value="<%= product.price %>" class="form-control" required><br>

                            <label for="description">Description:</label><br>
                            <textarea id="description" name="description" rows="4" cols="50" class="form-control" required><%= product.description %></textarea><br>
            
                            <label for="image">Current Image:</label><br>
                            <input type="text" name="currentImage" value="<%= product.image %>" class="form-control" disabled><br><br>
                            <img src="/images/<%= product.image %>" width="20%"><br><br>
            
                            <label for="image">New Image:</label><br>
                            <input type="file" id="image" name="image" accept="image/*" class="form-control"><br>
            
                            <button type="submit" class="btn submit-btn">Update Product</button>
                        </form>
                    <% } else { %>
                        <p>No product found.</p>
                    <% } %>
                    <a href="/adminView">Back</a>
                </div>
            </div>
        </div>

    </div>
</body>
</html>
